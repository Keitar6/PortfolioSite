import styled from 'styled-components';
import { StandardFlexContainer } from '../../global.styles';
import { motion } from 'framer-motion';

export const HpClickContainer = styled(StandardFlexContainer)`
  width: 100%;
  padding-bottom: 1rem;
`;

export const HpClickWrapper = styled(motion.div)`

`;
